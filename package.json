{
  "name": "screenshot-api",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "npx puppeteer browsers install chrome && node server.js"
  },
  "dependencies": {
    "capture-website": "^5.0.0",
    "express": "^4.18.2"
  }
}